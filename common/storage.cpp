/*#include "storage.h"

template <typename Id, typename T>
Storage<Id,T>::Storage()
{
    //this->storage = new QMap<Id, T>();
}

/*template <class Id, class T>
void Storage::add(Id id, T v) {
    //storage.insert(id, v);
}

template <class Id, class T>
void Storage<Id,T>::update(Id id, T v){
    storage[id] = v;
}

template <class Id, class T>
void Storage<Id,T>::remove(Id id){
    storage.remove(id);
}

template <class Id, class T>
bool Storage<Id,T>::contains(Id id){
    storage.contains(id);
}

template <class Id, class T>
T Storage<Id,T>::get(Id id){
    return storage[id];
}

template <class Id, class T>
QList<T> Storage<Id,T>:: list(){
    return storage.values();
}*/
