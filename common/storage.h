/*#ifndef STORAGE_H
#define STORAGE_H

#include <QMap>
#include <QList>

template <typename Id, typename T>
class Storage
{
public:
    Storage();
    //template <class Id, class T>
    //void add(Id id, T v);
    //void update(Id id, T v);
    //void remove(Id id);
    //bool contains(Id id);
    //T get(Id id);
    //QList<T> list();
private:
    //QMap<Id, T> storage;

};

#endif // STORAGE_H*/
